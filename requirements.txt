# Runtime dependencies for the SagaMind core service.
# Single source of truth is pyproject.toml; this file mirrors [project.dependencies]
# for the container build. For development install the extras instead:
#     pip install -e ".[dev,dashboard,wasm,grpc,llm]"
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
z3-solver>=4.12.0
numpy>=1.23.0
psycopg2-binary>=2.9.6
pgvector>=0.2.0
neo4j>=5.8.0
